/**
 *
 * il y a 3 grands type de variable , type string, number et booléen
 * 
 */

let prenom = "yannick"; /**string */
let years = 24;/**number */
let bool = true; /**booléen */

/** on utilise let et const pour déclarer une variable , 
 * et var n'est plus utiliser car déprécier*/
/** variable basique , on utilise let  */
let name = "yannick";
/**variable qui ne change jamais de valeur avec const , 
 * et ne peut être réassigner */
const lastname = "courbic";

/**const très utiliser , utiliser en le plus 
 * possible pour avoir moins  d'erreur*/


/**nommage des variables , les variables doivent 
 * êtres explicite , on peut écrire en camelCase  le premier mot 
 * en minuscule + duexième mot en maj pour 1er lettre */

const températureMeteo = 0;
/**on évite d'utiliser des caractères spéciaux */
