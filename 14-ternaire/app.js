/**
 * *les opértauers ternaire , 
 * *ce sera des expressions plus des blocs
 * * c'est une expresions qui va poser des cconditions 
 * *condition ? vrai alors exp: faux alors exp 
 * 
 */

let prix = 500;
//* c'est une expression sous forme de condition
let choix = prix > 200 ? "Premium" : "Basique";
//* on utilisera if et else , quand ce sera des conditions simple
//*si cela devient complexe , mieux vaut utiliser les else et if sous 
//*sous forme classique
console.log(choix);